export interface Bird {
  id: number;
  name: string;
  species: string;
  description: string;
  image: string;
  audio: string;
}

export type LevelsEn = 'warming' | 'sparrows' | 'forest birds' | 'songbirds' | 'raptors' | 'seabirds';
